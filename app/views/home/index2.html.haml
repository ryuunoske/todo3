%div
  %div
    %div
      %h1
        Welcome
        \#{current_user.username}
      %p Here you can manage your daily tasks in a nice manner
%br/
%div
  - if @tasks.empty?
    %span There are no tasks!
  - else
    %table
      %tr
        %thead
          %th Title
          %th Created at
          %th Completed
      %tbody
        - @tasks.each do |task|
          %tr
            %td
              %strong
                = task.title
            %td
              = task.created_at
            %td
              = task.completed
- if user_signed_in?
  = link_to "Sign Out", destroy_user_session_path, method: :delete
